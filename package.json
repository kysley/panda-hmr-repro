{
  "name": "@acme/ui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "echo TODO: add vitest",
    "packages:lint": "turbo run lint --filter=./packages/*",
    "packages:test": "turbo run test --filter=./packages/*"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.0",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=18.0.0"
  },
  "packageManager": "yarn@1.22.17",
  "dependencies": {
    "jest": "^29.3.1"
  }
}
